text="agbhfgth"
pattern="fgt"
m=len(pattern)
hash=0
for ch in pattern:
    hash=hash+ord(ch)
for i in range(len(text)-m+1):
    hash2=0
    for j in range(m):
        hash2=hash2+ord(text[i+j])
    if hash==hash2:
        print("Pattern found at index ",i)

